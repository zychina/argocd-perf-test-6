apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app70-svc
  namespace: clawtest
  labels:
    app: perf-test6-app70
spec:
  selector:
    app: perf-test6-app70
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
