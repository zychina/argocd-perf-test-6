apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app897-svc
  namespace: clawtest
  labels:
    app: perf-test6-app897
spec:
  selector:
    app: perf-test6-app897
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
