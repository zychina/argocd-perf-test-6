apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app284-svc
  namespace: clawtest
  labels:
    app: perf-test6-app284
spec:
  selector:
    app: perf-test6-app284
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
