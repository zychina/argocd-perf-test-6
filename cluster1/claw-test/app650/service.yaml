apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app650-svc
  namespace: clawtest
  labels:
    app: perf-test6-app650
spec:
  selector:
    app: perf-test6-app650
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
