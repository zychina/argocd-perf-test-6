apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app997-svc
  namespace: clawtest
  labels:
    app: perf-test6-app997
spec:
  selector:
    app: perf-test6-app997
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
