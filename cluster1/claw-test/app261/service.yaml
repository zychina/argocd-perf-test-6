apiVersion: v1
kind: Service
metadata:
  name: perf-test6-app261-svc
  namespace: clawtest
  labels:
    app: perf-test6-app261
spec:
  selector:
    app: perf-test6-app261
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
